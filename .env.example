# Copy to .env and adjust. Keep MYSQL_ROOT_PASSWORD / MYSQL_DATABASE / MYSQL_PORT
# in sync with the tms.Url / tms.User / tms.Pass values in Settings.ini.
MYSQL_ROOT_PASSWORD=changeme
MYSQL_DATABASE=e113
MYSQL_PORT=3306
